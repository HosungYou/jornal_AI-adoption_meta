# 04 Three-Way Comparison

Stores field-level `H-C-L` comparison tables, agreement-pattern summaries, and
error taxonomy outputs.

Use `paper_c/templates/three_way_comparison_template.csv` as the starting
template.
