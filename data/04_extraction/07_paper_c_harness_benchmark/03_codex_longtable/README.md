# 03 Codex LongTable

Stores Codex + LongTable harness extraction outputs and validation summaries.

Required harness artifacts include source spans, uncertainty flags, schema
validation, correction history, checkpoint/decision traces, and rerun manifests.
