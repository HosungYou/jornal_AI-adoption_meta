# 00 Manifest

Stores the 213-study Paper C corpus manifest.

Use `paper_c/templates/corpus_manifest_template.csv` as the starting template.
The manifest should avoid private PDF hyperlinks in shareable versions.
