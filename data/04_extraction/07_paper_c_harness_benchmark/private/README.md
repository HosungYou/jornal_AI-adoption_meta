# Private Local Artifacts

This folder is ignored by Git.

Use it for local-only PDFs, private run logs, raw model transcripts, and
non-redacted rerun bundles. Do not rely on this folder for shareable
reproducibility artifacts.
